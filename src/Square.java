public class Square {
    public static int square(double a ) {
/** 1.Напишіть метод, який приймає ціле число як аргумент і виводить його квадрат*/
        System.out.println("квадрат чисда "+ a +" = "+(int) a * a);
        return (int) (a * a);

    }
}
